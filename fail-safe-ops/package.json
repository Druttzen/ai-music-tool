{
  "name": "@aimc/fail-safe-ops",
  "version": "0.2.0",
  "private": true,
  "description": "Fail-Safe Ops — GitHub CI diagnose, auto-fix playbooks, review fallback (CLI + local UI shell)",
  "bin": {
    "fail-safe-ops": "./bin/fail-safe-ops.cjs"
  },
  "scripts": {
    "ui": "node ./bin/fail-safe-ops-ui.cjs"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  }
}
