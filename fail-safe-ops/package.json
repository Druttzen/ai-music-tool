{
  "name": "fail-safe-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Fail-Safe Ops — GitHub CI diagnose, auto-fix playbooks, review fallback (CLI-first)",
  "bin": {
    "fail-safe-ops": "./bin/fail-safe-ops.cjs"
  },
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  }
}
