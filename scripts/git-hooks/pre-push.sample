#!/bin/sh
# Copy to .git/hooks/pre-push or run: npm run hooks:install
if [ "$SKIP_CI_PREFLIGHT" = "1" ]; then
  echo "pre-push: SKIP_CI_PREFLIGHT=1 — skipping check:ci"
  exit 0
fi
echo "pre-push: running npm run check:ci"
npm run check:ci || exit 1
